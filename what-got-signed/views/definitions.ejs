<!DOCTYPE html>
<html lang="en" class="wa-cloak">
<head>
  <%- include('partials/head', { title: 'Definitions - What Got Signed?' }) %>
</head>
<body>
  <div class="page">
    <div class="content">
      <%- include('partials/header', { isHome: false }) %>

      <h2 class="detail-title">Definitions</h2>

      <wa-tab-group>
        <wa-tab slot="nav" panel="themes">Themes</wa-tab>
        <wa-tab slot="nav" panel="populations">Populations</wa-tab>

        <wa-tab-panel name="themes">
          <p class="wa-color-text-quiet">Themes are policy areas that executive orders address. Each order can have multiple themes.</p>
          <div id="themes-list" class="definitions-list"></div>
        </wa-tab-panel>

        <wa-tab-panel name="populations">
          <p class="wa-color-text-quiet">Populations are groups of people, organizations, or entities that may be impacted by executive orders.</p>
          <div id="populations-list" class="definitions-list"></div>
        </wa-tab-panel>
      </wa-tab-group>

      <%- include('partials/back-to-top') %>
    </div>
  </div>

  <%- include('partials/footer') %>

  <script src="/definitions.js"></script>
</body>
</html>
